#include <stdio.h>

int main() {
    int q;
    scanf("%d", &q);  // Number of test cases

    for (int i = 0; i < q; i++) {
        unsigned int n;
        scanf("%u", &n);
        printf("%u\n", ~n);  // Flip bits using bitwise NOT
    }

    return 0;
}
